# Sanity (required for build:local, preview, and publish)
# Get project ID and dataset from https://sanity.io/manage
SANITY_PROJECT_ID=your-project-id
SANITY_DATASET=production

# Optional: for publishing to GitHub (not needed for local build/preview)
# GITHUB_TOKEN=
# GITHUB_REPOSITORY=owner/repo

# Optional: for local build only (npm run build:local / preview)
# OUTPUT_DIR=dist
# BASE_PATH=
